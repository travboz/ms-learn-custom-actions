name: Setup custom CLI tooling in a custom action
description: Installs MyCLI and adds it to the PATH
author: travboz

inputs:
  version:
    description: The version of `golangci-lint` to install
    required: true

runs:
  using: node16
  main: index.js
